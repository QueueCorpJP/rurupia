
export function RecentSales() {
  // This is a placeholder component that doesn't render anything visible
  // It's just here to satisfy the import in AdminDashboard.tsx
  return <div className="hidden"></div>;
} 
